#!/usr/bin/env bash
script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"

sudo apt install -y wget2 curl

# Default login shell for future sessions
sudo chsh -s "$(command -v zsh)"
sudo chsh -s "$(command -v zsh)" "$(whoami)"

# Source common utilities
source "$script_dir/runs/utils.sh"

# Because Git submodule commands cannot operate without a work tree, they must
# be run from within $HOME (assuming this is the root of your dotfiles)
cd "$HOME" || exit 1

git config --global user.email "i@inomoz.ru"
git config --global user.name "Inom T."

echo
echo "Select bootstrap runner:"
echo "  1) desktop"
echo "  2) laptop"
echo "  3) wsl"
echo "  4) skip"

while true; do
    read -r -p "Runner #: " opt
    case "$opt" in
        1 | desktop)
            opt=desktop
            ;;
        2 | laptop)
            opt=laptop
            ;;
        3 | wsl)
            opt=wsl
            ;;
        4 | skip)
            log_info "Skipped. Run ~/.config/yadm/desktop, laptop, or wsl when ready."
            exit 0
            ;;
        *)
            echo "Invalid choice."
            continue
            ;;
    esac

    log_info "Starting $opt bootstrap..."
    bash "$script_dir/$opt"
    exit $?
done

# Sync submodules
yadm submodule update --recursive --init
