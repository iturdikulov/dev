#!/usr/bin/env bash

source "$(dirname -- "${BASH_SOURCE[0]}")/utils.sh"

sudo apt remove firewalld kmail akregator kaddressbook
sudo apt install -y aria2 wget2 curl thunderbird rsync nfs-common kdeconnect sshfs openssh-server autossh msmtp msmtp-mta filezilla whois qbittorrent

sudo apt install -y samba
sudo systemctl disable samba-ad-dc
sudo systemctl mask samba-ad-dc

# WARN: Enable firewall for debian!
sudo apt install -y ufw
sudo systemctl enable ufw.service
sudo ufw default deny incoming
sudo ufw default allow outgoing

sudo ufw allow ssh
sudo ufw allow nfs
sudo ufw allow samba

# Proxy
sudo ufw allow 2080/tcp
sudo ufw allow 2080/udp

# KDE connect rules
sudo ufw allow 1716:1764/tcp
sudo ufw allow 1716:1764/udp

# Valutwarden
sudo ufw allow 8000/tcp

# Jellyfin
sudo ufw allow 8096/tcp
sudo ufw allow 8920/tcp
sudo ufw allow 7359/udp

# Custom
sudo ufw allow 8080/tcp
sudo ufw allow 9090/tcp

sudo ufw enable
sudo ufw status verbose
