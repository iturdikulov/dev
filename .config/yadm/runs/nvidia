#!/bin/sh
cd /tmp || exit 1

sudo apt install -y firmware-linux  firmware-linux-nonfree  firmware-misc-nonfree

wget2 --progress=bar https://developer.download.nvidia.com/compute/cuda/repos/debian12/x86_64/cuda-keyring_1.1-1_all.deb
sudo dpkg -i cuda-keyring_1.1-1_all.deb
sudo apt update
rm -f cuda-keyring_1.1-1_all.deb
sudo apt install -y cuda nvidia-kernel-open-dkms nvidia-open

# Install LACCT
wget2 --progress=bar https://github.com/ilya-zlobintsev/LACT/releases/download/v0.8.0/lact-0.8.0-0.amd64.debian-12.deb
sudo dpkg -i lact-0.8.0-0.amd64.debian-12.deb && rm lact-0.8.0-0.amd64.debian-12.deb

echo 'GRUB_CMDLINE_LINUX="$GRUB_CMDLINE_LINUX nvidia-drm.modeset=1 nvidia-drm.fbdev=1"' | sudo tee /etc/default/grub.d/nvidia-modeset.cfg
echo 'options nvidia NVreg_PreserveVideoMemoryAllocations=1' | sudo tee /etc/modprobe.d/nvidia-power-management.conf

echo "Check after reboot cat /sys/module/nvidia_drm/parameters/modeset, cat /proc/driver/nvidia/params | grep PreserveVideoMemoryAllocations should return Y and 1"

# Enable power-management services
sudo systemctl disable --now nvidia-suspend.service
sudo systemctl disable --now nvidia-hibernate.service
sudo systemctl disable --now nvidia-resume.service

echo "Check https://docs.coolercontrol.org/installation/debian.html for coolercontrol"
