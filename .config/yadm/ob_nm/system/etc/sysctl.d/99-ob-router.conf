# ob_nm router: enable packet forwarding so LAN clients reach the WAN.
net.ipv4.ip_forward=1
net.ipv6.conf.all.forwarding=1
# TUN/auto-redirect: avoid dropping TCP return packets from the Mihomo interface.
net.ipv4.conf.all.rp_filter=0
net.ipv4.conf.default.rp_filter=0
