#!/bin/sh
# TODO: run on laptop!

trash ~/.local/share/applications/freecad.desktop
trash ~/.local/bin/freecad

sudo apt remove -y cuda-keyring cuda-drivers
sudo apt remove -y cuda-drivers cuda-toolkit
sudo apt remove --purge nvidia-installer-cleanup \
      nvidia-persistenced
sudo apt purge nvidia-*
sudo apt purge nvidia-driver-*
sudo apt purge cuda-* libcublas-* libcuda1 libcufft-* libcufile-* libcurand-* libcusolver-* libcusparse-* libglx-nvidia* libnvidia-* xserver-xorg-video-nvidia
sudo apt autoclean
sudo apt autoremove --purge

sudo apt remove -y v4l2loopback-dkms v4l-utils yt-dlp


