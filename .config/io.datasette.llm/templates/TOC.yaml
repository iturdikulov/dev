prompt: >
    Generate a hierarchical table of contents for the following text text. Use appropriate heading levels (H1, H2, H3, etc.). Include page numbers if present. Return only the table of contents.
